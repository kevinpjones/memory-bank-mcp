import {
  GrepFileParams,
  GrepFileResult,
  GrepFileUseCase,
  GrepMatch,
} from "../../../domain/usecases/index.js";
import { FileRepository, ProjectRepository } from "../../protocols/index.js";

export type {
  FileRepository,
  GrepFileParams,
  GrepFileResult,
  GrepFileUseCase,
  GrepMatch,
  ProjectRepository,
};
